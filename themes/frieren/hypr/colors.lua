local palette = require("catppuccin").mocha()

return {
    accent1 = palette.green.hex,
    accent2 = palette.red.hex,
    accent3 = palette.teal.hex,
    accent4 = palette.yellow.hex,
    inactive = palette.blue.hex,
    shadow = palette.mantle.hex,
}
